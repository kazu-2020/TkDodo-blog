<% content_for(:title, @faq_page_data[:name]) %>

<div
  class="embed rounded-sm shadow-md text-white bg-gray-750 border border-gray-450"
>
  <a href="<%= "https://www.nhk.jp/p/ts/#{@episode_data.dig(:identifierGroup, :seriesId)}/faqpage/#{@faq_page_data[:url]}" %>" target="_blank">
    <div class="p-3 grid grid-cols-12 gap-3">
      <div class="col-span-7">
        <div>
          <span class="text-xs" style="color: <%= @episode_data.dig(:partOfSeries, :style, :primaryLight) %>"><%= @episode_data.dig(:identifierGroup, :seriesName) %></span>
          <span class="text-gray-350 text-xs"><%= @episode_data[:name] %></span>
        </div>
        <div class="font-bold text-lg mt-1 mb-2"><%= @faq_page_data[:name] %></div>
        <div class="text-gray-350 text-sm">
          <%= @faq_page_data[:description] %>
        </div>
      </div>
      <div class="col-span-5 pl-4">
        <img
          decoding="async"
          src="<%= @episode_data.dig(:eyecatch, :medium, :url) %>"
          data-v-7e59b65e=""
          class="rounded-sm"
          />
      </div>

      <div class="questions col-span-12">
        <div
          class="col-span-12 border-t border-gray-350 pt-2"
        >
          <ol class="list-none text-gray-350">
            <% @faq_page_data[:questions].take(2).each do |question| %>
              <li class="mb-1">
                <div class="question flex text-white mb-2">
                  <span class="material-icons">help</span>
                  <span class="ml-2"><%= question[:name] %></span>
                </div>
                <div class=answer><%= question[:acceptedAnswer].first[:text] %></div>
              </li>
            <% end %>
          </ol>
        </div>
      </div>
    </div>
    <%= render 'shared/embed_footer' %>
  </a>
</div>
