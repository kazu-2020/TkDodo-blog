version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - amplify publish
        # - export SENTRY_PROJECT_NAME="editorialhands"
        # - export GIT_REF=$(git describe --always)
        # - export SENTRY_RELEASE_VERSION="${SENTRY_PROJECT_NAME}@${GIT_REF}"
        # - VERSION=$SENTRY_RELEASE_VERSION yarn build:amplify
        # - if [ "${AWS_BRANCH}" = "master" ]; then ./bin/release_sentry.sh ; fi
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: dist/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
